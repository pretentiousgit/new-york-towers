export default function matrix(c, r) {
  const arr = [];
  for (let i = 0; i < c; i += 1) {
    arr.push([]);
    for (let j = 0; j < r; j += 1) {
      // eslint-disable-next-line no-unused-expressions
      arr[i].push[0];
    }
  }
  return arr;
}
